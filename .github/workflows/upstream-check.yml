name: Upstream Update Checker

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check upstream KSU/SUSFS commits
        run: bash scripts/upstream_check.sh
